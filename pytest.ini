[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = .
markers =
    unit: fast unit tests
    integration: tests that touch filesystem / KB
    slow: tests that take > 1s
addopts = -v --tb=short
